# n8n業務自動化エージェント環境

このフォルダは、社内業務自動化（ヒアリング→設計→実装→検証→デプロイ→運用改善）を
n8nワークフローとして構築するための、Claude Code専用環境です。

このフォルダの外にある `../CLAUDE.md`（コウキAIラボ）とは独立しています。
このフォルダ配下で作業するときは、この CLAUDE.md の方針を優先してください。

## 参照ルール（@import）

以下は常に読み込んでください。Claude Codeの `@path` インポート構文で自動読込されます。

@.claude/rules/security.md
@.claude/rules/n8n-workflow-json.md
@.claude/rules/documentation.md

## 品質方針

「開発速度優先」ではなく、**セキュリティ・保守性・拡張性・信頼性を含む高度な最適化重視**。

## ハルシネーション防止（絶対条件）

- n8nノードの正式名称・`type`・`typeVersion`・パラメータ・Credential Type・API仕様・
  実行結果・デプロイ結果を**推測で生成しない**。
- 確認できない項目は `[未確認]` `[要公式確認]` `[要インスタンス確認]` `[ユーザー入力待ち]`
  `[実行環境なしのため未テスト]` のいずれかを明記する。
- 情報源の優先順位：①接続先n8nインスタンスの実データ ②n8n公式ドキュメント
  ③n8n公式GitHub ④Anthropic公式Claude Codeドキュメント ⑤外部サービス公式APIドキュメント
  ⑥公式SDK/パッケージ。非公式ブログ・SNS・動画・掲示板は補助情報のみ。
- すべての調査・設計結果は「確認済み事実／現在の仮定／未確認事項／推奨案／代替案／判断理由」
  に分けて記述する。「おそらく」「一般的には」だけで仕様を確定しない。
- コマンド・API・ワークフローを実際に実行していない場合、「実行済み」「正常に動作する」
  「デプロイ完了」と表現しない。テスト結果には実行コマンド・日時・対象環境・入力・実際の出力・
  成否・残存リスクを含める。

## エージェント構成（`.claude/agents/`）

| エージェント | 役割 | 権限の目安 |
|---|---|---|
| `n8n-automation-lead` | 全体責任者。要件整理・タスク分解・委任・承認ポイント管理・デプロイ可否判断 | 読み書き（設計文書）、実装・本番操作は他エージェントへ委任 |
| `n8n-schema-researcher` | 原則読み取り専用。n8n公式仕様・接続先バージョン・ノード仕様・API仕様の確認 | 読み取り専用（Web検索・インスタンス読み取りAPIのみ） |
| `n8n-workflow-builder` | 確認済み仕様をもとにワークフローJSON・補助コード・テストデータを実装 | ファイル作成・編集（`workflows/draft/` `tests/` 配下） |
| `n8n-quality-auditor` | 実装者と独立した品質監査。原則読み取り専用 | 読み取り専用。重大な問題があればデプロイ非承認 |

複数社員（複数エージェント）へ同時に仕事を振るときは、並列で起動する。

## 権限・安全設計

### 自動許可してよい操作
プロジェクト内のファイル参照・検索・静的解析・JSON構文検証・テストファイル作成・
ドキュメント作成・Git差分確認・読み取り専用APIアクセス。

**【2026-08-15、社長承認・恒久化】** 以下も自動許可に追加する：n8nワークフローの
新規登録・更新・有効化／無効化、既存Credential（実IDが判明しているもの）の
ワークフローへの割当、本番Webhook・環境変数のうちn8nワークフロー自体の設定に伴うもの、
Credentialの新規作成（ただし秘密の値自体はチャットへ貼らせず、必ずn8n UI上でユーザー本人が
入力する。Claudeが作成できるのは参照設定までで、OAuth同意画面の操作等ユーザー本人の
操作が技術的に必須な部分は引き続きユーザーが行う）。
背景：2026-08-15のPUB-004実機テストで、都度承認を挟む運用だと反復デバッグの効率が
著しく落ちること、およびAPIの制約上むしろCredential参照の再設定こそ都度承認が発生しやすい
実務上のボトルネックだったことを踏まえ、社長より本番接続・Credential作成を含めた完全自動化の
指示があり、本ルールを恒久的に変更した。**ワークフロー・Credentialの削除は対象外とし、
引き続き毎回の承認を必須とする**（意図的に対象から除外。取り消しが効かない操作のため）。

### 毎回承認が必要な操作
n8nワークフロー・Credentialの**削除**、Git push、本番ブランチへのマージ、DB変更、
外部システムへの書き込み（n8nワークフロー経由の通常動作を除く）、メール／チャット／通知の
本送信（n8nワークフロー経由でのテスト実行・実運用配信を除く。2026-08-15の方針変更により
n8nワークフロー自体の実行・配信は自動許可対象）、有料APIの大量実行、個人情報を含む
データ処理、ファイル削除、システム設定変更。

### 原則禁止
シークレットのハードコード、`.env`のGit登録、認証情報を含むワークフローJSONの保存、
静的検証（`validate-workflow.mjs`・`check-secrets.mjs`）を経ない本番有効化、
バックアップなしの既存ワークフロー更新、出所不明なMCPサーバー／npmパッケージの導入、
安全性未確認のシェルスクリプト実行、ユーザー承認なしの破壊的変更（削除操作）、
権限確認を回避する設定（`bypassPermissions`相当）の利用。
自動化対象になったとはいえ、ワークフローの新規登録・更新・有効化の前には必ず
`validate-workflow.mjs`・`check-secrets.mjs`を実行し、結果を成果物として残す
（本ルール変更は承認プロセスの省略であり、品質チェックの省略ではない）。

上記は `.claude/settings.json` の `permissions` でも技術的に制御する（詳細は同ファイル参照）。
Hooksは今回**未実装・無効**（動作未検証のHooksは有効化しない方針）。導入する場合は
`docs/troubleshooting.md` の検証手順に従うこと。

## 標準作業フロー（Phase 0〜10）

Phase 0 環境確認 → 1 業務分析 → 2 要件定義 → 3 設計（2案以上比較）→ 4 実装 →
5 静的検証 → 6 テスト → 7 ステージング → 8 本番移行（要承認）→ 9 運用 → 10 最適化。
詳細は `docs/architecture.md` を参照。

## ディレクトリの役割

- `templates/` … ヒアリングから運用手順書までの雛形
- `workflows/{draft,validated,deployed,archived}/` … ワークフローJSONのライフサイクル別置き場
- `tests/{fixtures,cases,results}/` … テストデータ・テストケース・実行結果
- `scripts/` … 検証スクリプト（`validate-workflow.mjs` など）
- `docs/` … アーキテクチャ・セキュリティ方針・命名規則・トラブルシューティング

## 現在確認できている環境

**【2026-08-16更新】旧版との矛盾を解消。** 以前の版では本節に「n8n環境：`[ユーザー入力待ち]`
（未確認・未接続）」という記述が残っていたが、これは環境接続後に更新されていなかった
**更新漏れ**だった（2026-08-16、`kohomada_2026-08-16_NoimosAI相当自動化設計書_v1.md`の指摘、
エイトが本節を修正）。以下は「過去セッションで確認済みの事実（出典付き）」と
「本セッション（このClaude Codeプロセス）自身が今この場で確認できること」を分けて記載する。
**セッションが変わるとサンドボックス環境そのものが変わりうる**ため、n8n接続可否は
毎回のセッション冒頭（Phase 0）で必ず自分で再確認し、下の「過去の確認済み事実」を
鵜呑みにして「今回も繋がっている」と仮定しない。

### 過去セッションで確認済みの事実（`[確認済み事実]`、出典付き）

- n8n環境：セルフホスト、ベースURL `https://kohomadha-n8n.top`（社長の環境）。API接続実績あり
  （`X-N8N-API-KEY`ヘッダーによる読み取り専用GET、および書き込み系API呼び出しの両方の実績を確認）。
  出典：`n8n-automation/docs/cases/AUTO-KHM-001/workflow-design.md`（2026-08-15、
  `GET /api/v1/workflows/{id}`での読み取り実績）、`n8n-automation/workflows/README.md`
  （2026-08-10・2026-08-15更新、複数ワークフローの`active:true`・Credential割当済みを確認した記録）。
- 本番稼働中（`active:true`）のワークフロー実績：`AUTO-CNT-001`（Notion Credential割当済み）、
  `AUTO-CNT-002`（Notion/Google/LINE Credential割当済み。ただし監査未実施のまま稼働していた
  経緯があり要注意）、`AUTO-COM-001`（Anthropic Credential割当済み、実ワークフローID
  `r0IZ2ByR7MX4RWCp`）、`AUTO-COM-002`（LINE/Notion Credential割当済み）。
  出典：`n8n-automation/workflows/README.md`「現在の状態一覧」表。
- 実機End-to-Endテスト成功実績：`PUB-004`（実ワークフローID`jejKmWHPDWSUifDZ`）で2026-08-15、
  RSS取得（45件）→フィルタ→AI要約・翻訳（Anthropic Claude）→Slack投稿→Google Sheets
  配信ログ書き込みまで完全成功（実行ID#251）。出典：
  `n8n-automation/docs/cases/AUTO-KHM-001/workflow-design.md`「2026-08-15：PUB-004
  実機テストで発見・修正した不具合」節。
- n8nバージョン固有の仕様（例：Webhookノードのversion配列`[1,1.1,2,2.1]`・defaultVersion 2.1、
  Notion `data_sources`クエリエンドポイントの採用等）は、上記の実装過程でn8n公式ドキュメント・
  公式GitHubと突き合わせながら個別に確認された値であり、本節で一括して「n8nのバージョン」
  そのものを断定するものではない（正確なn8nセルフホストのバージョン番号自体は
  `[未確認]`のまま。`GET /api/v1/...`が動作する程度の比較的新しいバージョンと推測されるに留まる）。

### 本セッション自身が確認できること（このセッションの実行環境）

- OS/シェル：このセッションの実行環境は`Platform: linux`（Claude Code起動時の環境情報より）。
  旧版に記載されていた「OS: Windows 11 Pro／シェル: Windows PowerShell 5.1」は、
  **別セッション（社長ご自身のPC上でClaude Codeを実行した際）のPhase 0確認結果**であり、
  現在このタスクを処理しているセッションの実行環境とは異なる可能性が高い。
  Node.js/npm/Gitの具体的なバージョンは、本セッションではコマンド実行手段
  （Bashツール等）が無く未確認（`[未確認]`）。
- n8n接続情報（`.env`）：本セッションの作業ディレクトリを確認したが、`n8n-automation/.env`
  ファイルは**存在しなかった**（`.env.example`のみ存在）。したがって、上記「過去セッションで
  確認済みの事実」に記載した接続実績を、**本セッション自身がこの場で再現・再検証することは
  できていない**。これは「n8nが未接続になった」ことを意味するものではなく、単に
  「今のセッションの作業ディレクトリに`.env`が無いため、このセッションからは直接APIを
  叩けない」という技術的制約である（過去に接続実績があった環境と、現在のサンドボックスが
  同一の永続ファイルシステムを共有しているとは限らない）。
- 結論：n8n本番環境自体は実在し、社長の環境から接続・登録・実機テストされた確かな実績が
  複数の一次記録（`workflows/README.md`・各`docs/cases/*/workflow-design.md`）に残っている。
  一方、**「今この瞬間のセッションから」n8n APIへ疎通できるかどうかは、セッションごとに
  `.env`の有無・実行環境を確認しないと分からない**。本セッションでは`.env`が無いため、
  ワークフローの新規登録・Credential操作等の**実際の実行（本番反映）はできない**。
  実装作業は`workflows/draft/`へのJSON作成・静的検証にとどめ、本番登録は`.env`が使える
  セッション（社長ご自身の環境等）で改めて実施する。
- テスト環境・本番環境の有無：`kohomadha-n8n.top`が事実上の唯一の環境であり、
  ステージング環境が別途存在するかは`[未確認]`（過去の記録にも言及なし）。
- Claude Codeバージョン：`[未確認]`（本セッションでコマンド実行手段が無いため）。

n8n接続情報が使えないセッションでは、`n8n-schema-researcher`はn8n公式ドキュメント／
公式GitHubのみを根拠に調査し、インスタンス固有の値（ノードtype・typeVersion・
Credential種別など）はすべて`[要インスタンス確認]`として扱う。接続情報が使えるセッションでも、
未検証の値を確認済み扱いにしない。
