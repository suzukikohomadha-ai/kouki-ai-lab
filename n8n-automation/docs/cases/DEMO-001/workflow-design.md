# ワークフロー設計書（DEMO-001）

## 基本情報

- ワークフロー名：経費申請しきい値チェック（デモ）
- 一意の管理ID：DEMO-001
- 目的：入力金額がしきい値を超えるか判定し、ステータスを返す（デモ）
- 業務責任者：（架空）
- 技術責任者：n8n-workflow-builder
- 対象部署：（架空）
- トリガー：Manual Trigger（手動実行）
- 入力データ：`{ amount: number, threshold: number }`
- 出力データ：`{ amount, threshold, status: "要確認" | "自動承認" }`
- 前提条件：外部接続なし。n8nの標準ノードのみで完結する
- 利用サービス：なし
- 必要Credential：なし
- 実行頻度：デモのため手動実行のみ
- 想定件数／最大件数：デモのため対象外
- 想定実行時間：数秒未満（外部呼び出しがないため）
- 許容遅延：対象外
- エラー時の対応：入力値が数値でない場合はエラーとして停止する（デモの範囲）
- 手動対応への切替条件：対象外
- ログ方針：デモのため特別なログ設計はしない
- 保存期間：対象外
- 個人情報の有無：なし
- 監視項目：対象外（デモのため）
- 成功条件：入力に応じて正しい `status` が返る
- KPI：対象外
- ロールバック方法：対象外（本番登録していないため）
- 変更履歴：初版（デモ作成時）

## 案の比較

このデモは学習・動作確認目的のため、単一案のみ（比較は本番案件で実施）。

## 技術観点チェック

- 冪等性：手動実行・副作用なしのため考慮不要
- 入力値検証：`amount` / `threshold` が数値であることを確認する分岐を入れる
- タイムアウト・リトライ：外部呼び出しがないため対象外
- ログ・監視：デモのため未設計（本番化時に設計する）
- 個人情報のマスキング：扱うデータなし

## ノード構成（ドラフト）

| ノード名 | 役割 | 種別 | type / typeVersion | 備考 |
|---|---|---|---|---|
| Manual Trigger | 手動実行の起点 | 公式ノード | `n8n-nodes-base.manualTrigger` / `[要インスタンス確認]` | |
| Set: テスト入力 | デモ用の固定入力値を設定 | 公式ノード | `n8n-nodes-base.set` / `[要インスタンス確認]` | 実案件ではフォーム/Webhook入力に置き換え |
| IF: しきい値判定 | amount > threshold を判定 | 公式ノード | `n8n-nodes-base.if` / `[要インスタンス確認]` | |
| Set: 要確認 | status="要確認" を付与 | 公式ノード | `n8n-nodes-base.set` / `[要インスタンス確認]` | |
| Set: 自動承認 | status="自動承認" を付与 | 公式ノード | `n8n-nodes-base.set` / `[要インスタンス確認]` | |
| NoOp: 完了 | 終端ノード | 公式ノード | `n8n-nodes-base.noOp` / `[要インスタンス確認]` | |

`type` の文字列はn8n公式ドキュメントで一般的に案内されている名称を参考にしていますが、
接続先インスタンスでの動作確認はしていません（`[要インスタンス確認]`）。

## Credentialマッピング表

なし（このデモはCredentialを使用しない）

## 分類

- **確認済み事実**：n8nの一般的なノード構成として、Manual Trigger / Set / IF / NoOp が
  公式ドキュメントで案内されている（出典：n8n公式ドキュメント全般の記載に基づく一般的知識。
  ただし本セッション内で公式ドキュメントの該当ページを直接確認したわけではないため
  `[要公式確認]` として扱う）
- **現在の仮定**：しきい値は単一の固定値
- **未確認事項**：接続先n8nインスタンスでの `type` / `typeVersion` の正確な値
  （`[要インスタンス確認]`）
